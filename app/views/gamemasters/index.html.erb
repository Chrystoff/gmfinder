<h1>
    Gamemasters
    <%= user_signed_in? ? link_to('New Gamemaster', new_gamemaster_path, class: "btn btn-primary") : '' %>
</h1>

<% @gamemasters.each do |gamemaster| %>
  <p>
    <%= link_to gamemaster.name, gamemaster %>
  </p>
<% end %>
