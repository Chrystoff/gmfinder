<%= form_with(model: @gamemaster) do |f| %>
    <fieldset class="mb-2">
        <%= f.label :name %>
        <br>
        <%= f.text_field :name %>
    </fieldset>
    <fieldset class="mb-2">
        <%= f.submit class: 'btn btn-outline-primary' %>
    </fieldset>
<% end %>