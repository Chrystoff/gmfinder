<h1>
    Players
    <%= policy(Player).new? ? link_to('New Player', new_player_path, class: "btn btn-primary") : '' %>
</h1>

<% @players.each do |player| %>
    <p>
        <%= link_to player.name, player %>
    </p>
<% end %>
