<h1>
    Players
    <%= user_signed_in? ? link_to('New Player', new_player_path, class: "btn btn-primary") : '' %>
</h1>

<% @players.each do |player| %>
    <p>
        <%= link_to player.name, player %>
    </p>
<% end %>
