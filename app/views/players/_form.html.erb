<%= form_with(model: @player) do |f| %>
  <fieldset class="mb-2">
    <%= f.label :name %>
    <br>
    <%= f.text_field :name %>
  </fieldset>
  <fieldset>
    <%= f.submit %>
  </fieldset>
<% end %>
